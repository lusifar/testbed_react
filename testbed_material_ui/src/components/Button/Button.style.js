import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  root: {
    margin: "0, auto",
    padding: "0",
    width: "100%",
    height: "100%",
  },
  container: {
    paddingTop: "2rem",
    height: "100%",
    backgroundColor: "white",
  },
});

export { useStyles };
